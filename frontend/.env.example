# Template for environment variables. Copy this to .env.local and fill in values.
# These variables are exposed to the client via Vite's import.meta.env

VITE_API_BASE_URL=http://localhost:8080
VITE_APP_ENV=development
VITE_SENTRY_DSN=
VITE_GA_TRACKING_ID=
